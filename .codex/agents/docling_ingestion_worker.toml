name = "docling_ingestion_worker"
description = "Implements Docling PDF-to-Markdown ingestion."
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Use Docling for uploaded papers, especially scanned PDFs. Save Markdown path/status in SQLite. Preserve old parser until safe. Do not fabricate metadata.
"""
